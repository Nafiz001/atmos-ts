{
  "name": "atmos-weather-app",
  "version": "1.0.0",
  "description": "Vanilla TypeScript Weather Application",
  "scripts": {
    "build": "tsc",
    "vercel-build": "npm run build && node copy-static.js",
    "watch": "tsc --watch",
    "serve": "npx serve ."
  },
  "keywords": ["weather", "typescript", "vanilla"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
